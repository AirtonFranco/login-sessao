# Sessao Login com Vue JS

Login mantendo a sessão ativa com o Token JWT no armazenamento local do navegador.

Vue Router validando sessão ativa com os protetores globais do roteador

```
npm install

npm run serve
```
